/**
 * Scripture Builder showcase — the study → build → prove → master loop,
 * animated with a real doctrinal-mastery verse.
 */
import ScriptureBuilderShowcase from './ScriptureBuilderShowcase.svelte';

export { ScriptureBuilderShowcase };
export default ScriptureBuilderShowcase;
