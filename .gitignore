node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# pnpm install leftovers (sandbox quirk — see AGENT_DECISIONS.md)
_tmp_*

# Agent decisions log (uncommitted by design)
AGENT_DECISIONS.md

# Legacy scripture data — superseded by Flutter port (TASK-B-080).
# Never re-track. See CLAUDE.md "Scripture data — source of truth".
src/lib/data/passages.json

